type: specs.openrewrite.org/v1beta/recipe
name: com.yourorg.ListAllFilesAndDeleteJks
displayName: "List all files, then delete .jks"
description: "Print every file considered, show which .jks will be deleted, then delete them."
recipeList:
  # 1) Print every file path (all languages & binaries)
  - org.openrewrite.FindSourceFiles:
      filePattern: "**/*"
  # 2) Also print only the .jks files (so you see the exact targets)
  - org.openrewrite.FindSourceFiles:
      filePattern: "**/*.jks"
  # 3) Delete all .jks files
  - org.openrewrite.DeleteSourceFiles:
      filePattern: "**/*.jks"
