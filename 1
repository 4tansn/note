import re

def extract_jar_names(file_path):
    # Regular expression to match jar names
    jar_regex = re.compile(r'(?<=\:)(.*?)(?=\:)')

    jar_names = set()  # Using a set to avoid duplicates

    # Open the dependencies.txt file
    with open(file_path, 'r') as file:
        # Read each line in the file
        for line in file:
            # Find all matches of jar names in the line
            matches = jar_regex.findall(line)
            # Add found jar names to the set
            jar_names.update(matches)

    return jar_names

# Example usage
dependencies_file_path = 'dependencies.txt'
jar_names = extract_jar_names(dependencies_file_path)

# Print the extracted jar names
for jar_name in jar_names:
    print(jar_name)
